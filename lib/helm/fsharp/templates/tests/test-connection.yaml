apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fsharp.fullname" . }}-test-connection"
  labels:
    {{- include "fsharp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fsharp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
